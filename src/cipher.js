const cipher = {
    /*function: cipher.encode(inputText, inputOffset) {
        for(let i = 0; i < stringLength; i++){
        ([inputText].charCodeAt(i) + inputOffset)% 26
        }
    } */
};

export default cipher;
